# Phase 1 Inventory: Query Engine Legacy Deprecation

## Summary

This document is the maintained Phase 1 inventory for the live legacy and hybrid surfaces named in the query-engine legacy deprecation plan.
It records what is still authoritative on the current branch, what is only transitional, and what already looks archival or dead.

The evidence in this inventory is based on the current runtime entry points in `sead.query.api`, the authoritative Autofac registrations in `sead.query.api/Dependency.cs`, the live runtime and helper implementations in `sead.query.core`, `sead.query.composer`, and `sead.query.infra`, the current operational guidance in `docs/OPERATIONS.md`, and a repository-wide reference check for the named `deprecated/*.sql` assets.

## Classification Legend

| Classification | Meaning                                                                                                                                             |
|----------------|-----------------------------------------------------------------------------------------------------------------------------------------------------|
| Live           | The current supported runtime or supported operations still depend on this surface directly.                                                        |
| Transitional   | The surface still exists in authoritative code or operations, but mainly as a migration boundary, compatibility seam, or unresolved exception path. |
| Archived       | The surface is historical material kept in the repository, with no current authoritative runtime or operational role found in Phase 1.              |
| Dead           | The surface appears to be scratch or one-off material with no active references or supported role on the current branch.                            |

## Runtime Fallback And Hybrid Surface Inventory

| Surface                                  | Current role and reachability                                                                                                                                                                                                                                                                                                           | Classification | Disposition      | Removal prerequisite                                                                                                                                                      |
|------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------|------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `FacetContentService`                    | Authoritative `IFacetContentService` behind `api/facets/load`; reached through `FacetsController.Load(...) -> LoadFacetService.Load(...) -> FacetContentService.Load(...)`. Uses `ComposedFacetContentService.CanHandle(...)` and falls back to `CategoryCountService.Load(...)` when the composed path rejects the request.            | Live           | Migrate          | Composer must cover the supported facet-content matrix so the fallback branch can be removed without narrowing support accidentally.                                      |
| `CategoryCountService`                   | Legacy category-count engine used from `FacetContentService` fallback. It remains keyed by facet type, but Phase 2 has now moved its request-setup assembly onto `SupportedRequestQuerySetupFactory`, so it no longer invokes `QuerySetupBuilder` directly.                                                                             | Live           | Remove           | No supported facet-content request may rely on fallback, and the fallback engine should be removable without reopening request-setup migration work.                      |
| `ComposedFacetContentService`            | The target composer implementation for facet-content, but still a hybrid boundary because `CanHandle(...)` decides whether callers stay on composer or drop to legacy through `FacetContentService`.                                                                                                                                    | Transitional   | Keep temporarily | The composed path must become the only supported contract, after which the caller-side fallback routing and fallback-oriented error guidance can be removed.              |
| `ResultService`                          | Authoritative `IResultService` behind `api/result/load`; reached through `ResultController.Load(...) -> LoadResultService.Load(...) -> ResultService.Load(...)`. It delegates request planning to `IResultProjectionHandoffBuilder`, which is currently the composed builder.                                                           | Live           | Keep temporarily | Keep until the handoff layer is simplified after fallback and hybrid query-setup dependencies are removed.                                                                |
| `LegacyResultProjectionHandoffBuilder`   | Legacy result handoff still invoked when `ComposedResultProjectionHandoffBuilder.TryCreateRequest(...)` fails. Phase 2 has now moved its projection setup assembly onto `SupportedRequestQuerySetupFactory`, so it no longer invokes `QuerySetupBuilder` directly.                                                                      | Live           | Remove           | No supported result request may trigger the composed-builder fallback path.                                                                                               |
| `ComposedResultProjectionHandoffBuilder` | Authoritative result handoff registration, but still transitional because it logs and falls back to `LegacyResultProjectionHandoffBuilder` on unsupported requests. Phase 2 has now removed its supported-path `QuerySetupBuilder` dependency by routing composed result projection setup through `SupportedRequestQuerySetupFactory`.  | Transitional   | Migrate          | Remove `_legacyBuilder` fallback once unsupported result requests no longer need the legacy handoff boundary.                                                             |
| `QuerySetupBuilder`                      | Shared legacy query-building core no longer appears as a direct request-path caller in the current runtime services. After the latest Phase 2 slice, its remaining live footprint is limited to retained base classes, compatibility-oriented services, and explicit legacy infrastructure that has not yet been deleted.               | Transitional   | Migrate          | Remove the remaining base-class and compatibility dependencies so the legacy builder can be deleted without affecting fallback branch removal.                            |
| `BogusPickService`                       | Transitional compatibility service now retained for legacy-aligned helper flows and tests. Phase 2 has removed it from `LoadFacetService.Load(...)`, `LoadResultService.Load(...)`, the facet/result SQL probe commands, and its own internal `QuerySetupBuilder` dependency by routing it through `SupportedRequestQuerySetupFactory`. | Transitional   | Migrate          | Reduce it to an explicit compatibility-only boundary, or delete it once no retained flow depends on it.                                                                   |
| `CategoryInfoService`                    | Shared category-info base for keyed discrete and geo-polygon category-info services. Phase 2 has now removed its `QuerySetupBuilder` dependency by routing the shared path through `SupportedRequestQuerySetupFactory`, so category-info generation no longer keeps the legacy builder on the supported request path.                   | Transitional   | Keep temporarily | Remove or simplify the shared base after fallback-oriented category-count infrastructure is retired and the plugin boundary no longer needs a shared compatibility layer. |

## DI And Plugin Registration Inventory

| Surface                                                                                                             | Current role                                                                                                                                                                                                                                                                                                                  | Classification | Disposition      | Removal prerequisite                                                                                                                                                                   |
|---------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------|------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `Dependency.cs` registration of `QuerySetupBuilder` and `BogusPickService`                                          | Keeps the legacy builder and pick-normalization path available for retained compatibility surfaces. Phase 2 has removed their direct request-path use from the supported facet/result load paths, the probe commands, category-info generation, category-count generation, and legacy result setup assembly.                  | Transitional   | Migrate          | Remove the remaining base-class or compatibility-only uses and reduce `BogusPickService` to an explicit compatibility-only boundary.                                                   |
| `Dependency.cs` registration of `SupportedRequestQuerySetupFactory` and `SupportedRequestPickSanitizer`             | New Phase 2 supported-request boundary for request-time planning and invalid-pick normalization. These services keep supported controller load paths and composed result projection setup off the legacy builder while fallback and compatibility services still exist elsewhere.                                             | Live           | Keep temporarily | Remove or simplify this temporary boundary only after the fallback-only legacy services are retired and the runtime no longer needs a split between supported and compatibility paths. |
| `Dependency.cs` registration of `CategoryCountService` and `FacetContentService`                                    | Keeps the legacy facet-content engine reachable from the supported API through the top-level facet-content service.                                                                                                                                                                                                           | Live           | Migrate          | Remove the fallback branch from `FacetContentService` and retire `CategoryCountService` after composer-only facet-content parity.                                                      |
| `Dependency.cs` registration of `LegacyResultProjectionHandoffBuilder` and `ComposedResultProjectionHandoffBuilder` | Keeps legacy result handoff reachable because the composed handoff builder still delegates to the legacy builder on unsupported requests.                                                                                                                                                                                     | Live           | Migrate          | Prove all supported result requests compose successfully, then remove the legacy handoff registration and simplify the composed registration.                                          |
| `DiscreteFacetPlugin` registration groups                                                                           | The plugin now exposes explicit `RegisterLegacyRuntime(...)`, `RegisterComposerRuntime(...)`, and `RegisterSharedPlugin(...)` groups. The keyed category-count helper/compiler and generic category-info registrations remain legacy/fallback-facing, while the typed services remain available to the authoritative runtime. | Live           | Migrate          | Remove the legacy registration group once discrete fallback behavior is no longer required.                                                                                            |
| `RangeFacetPlugin` registration groups                                                                              | The plugin now exposes explicit legacy, composer, and shared registration groups. Range outer-bound and typed services remain composer-facing, while keyed category-count services still support fallback.                                                                                                                    | Live           | Migrate          | Remove the legacy registration group once range fallback behavior is no longer required.                                                                                               |
| `IntersectFacetPlugin` registration groups                                                                          | The plugin now exposes explicit legacy, composer, and shared registration groups so intersect fallback registrations can be removed separately from the typed services the current runtime still uses.                                                                                                                        | Live           | Migrate          | Remove the legacy registration group once intersect fallback behavior is no longer required.                                                                                           |
| `GeoPolygonFacetPlugin` registration groups                                                                         | The plugin now exposes explicit legacy, composer, and shared registration groups. Typed geo services remain part of the authoritative composed facet-content path, while keyed category-count services remain fallback-facing.                                                                                                | Live           | Migrate          | Remove the legacy registration group once geo-polygon fallback behavior is no longer required.                                                                                         |

## SQL Asset Inventory And Classification

| Surface                                                                                  | Current role and evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Classification | Disposition      | Removal prerequisite                                                                                                                                   |
|------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------|------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------|
| `scripts/prepare-phase5-facet-runtime-schema.sql`                                        | Still an active operational asset. `docs/OPERATIONS.md` describes it as the current schema-prep path for missing `facet.anchor`, `facet.facet_anchor`, `facet.facet_template`, `facet.route`, `facet.route_step`, and `facet.config_revision` runtime tables, and the script itself creates those tables and constraints idempotently.                                                                                                                                                                                                   | Live           | Keep temporarily | Replace it with a durable baseline migration path or confirm every supported target already carries the required runtime schema and provenance tables. |
| `deprecated/000_create_staging_db.sql`                                                   | Historical operational script that terminates connections and clones `sead_production` into `sead_staging`. No active references were found outside the deprecation proposal material.                                                                                                                                                                                                                                                                                                                                                   | Archived       | Archive          | None beyond keeping it clearly non-authoritative or moving it deeper into archival material.                                                           |
| `deprecated/cr_01_change_set_results.sql`                                                | Historical results-schema migration scratch for old `facet.result_*` tables. No active references were found outside the deprecation proposal material.                                                                                                                                                                                                                                                                                                                                                                                  | Archived       | Archive          | None beyond keeping it clearly non-authoritative or moving it deeper into archival material.                                                           |
| `deprecated/cr_02_change_set_bugfix.sql`                                                 | Historical results bugfix and sequencing script for `facet.result_*` tables. No active references were found outside the deprecation proposal material.                                                                                                                                                                                                                                                                                                                                                                                  | Archived       | Archive          | None beyond keeping it clearly non-authoritative or moving it deeper into archival material.                                                           |
| `deprecated/measured_value.sql`                                                          | One-off exploratory PL/pgSQL scratch for generating measured-value SQL. No active references were found outside the deprecation proposal material.                                                                                                                                                                                                                                                                                                                                                                                       | Dead           | Archive          | None.                                                                                                                                                  |
| `deprecated/UDF_bugg_29.sql`                                                             | One-off scratch query against `facet.method_measured_values(...)`. No active references were found outside the deprecation proposal material.                                                                                                                                                                                                                                                                                                                                                                                            | Dead           | Archive          | None.                                                                                                                                                  |
| `deprecated/value_diffs.sql`                                                             | Environment-specific diff scratch comparing `public` and `public_production`. No active references were found outside the deprecation proposal material.                                                                                                                                                                                                                                                                                                                                                                                 | Dead           | Archive          | None.                                                                                                                                                  |
| `deprecated/bugg_tests.sql`                                                              | Exploratory query scratch with trace output and manual investigation SQL. No active references were found outside the deprecation proposal material.                                                                                                                                                                                                                                                                                                                                                                                     | Dead           | Archive          | None.                                                                                                                                                  |
| Imported `sql_override` and anchor-level SQL overrides written to `facet.facet_template` | Still part of the imported configuration contract. The importer upserts `FacetTemplate` rows in `FacetRouteConfigurationImporter.UpsertFacetTemplates(...)`, the runtime schema-prep script still creates `facet.facet_template`, and the docs still describe SQL overrides as rare supported exceptions. The current non-archived code references only the entity model, EF mapping, and importer path; the only `Facet.GetTemplate(...)` request-path consumers found in Phase 2 are under `sead.query.composer/Archived/BackBurner/`. | Transitional   | Keep temporarily | Treat this as importer/schema compatibility to clean up in Phase 5 unless a non-archived runtime consumer is found later.                              |

## `facet_template` Runtime Reachability Proof

- The repository-wide non-archived code search found no live request-path consumer of `Facet.GetTemplate(...)`.
- Current non-archived references to `FacetTemplate` are limited to the entity model in `sead.query.core`, the EF mapping in `sead.query.infra/Repository/FacetContext.cs`, and the import path in `sead.query.infra/Configuration/FacetRouteConfigurationImporter.cs`.
- The only request-path consumers found for `Facet.GetTemplate(...)` are in `sead.query.composer/Archived/BackBurner/`, which is already archival material and not part of the authoritative runtime.
- The Phase 2 disposition is now explicit: treat `facet.facet_template` as importer/schema compatibility and Phase 5 cleanup scope, not as a live request-path blocker for Phase 2 or Phase 3.

## Findings That Change Later Phases

- Phase 2 has now removed `BogusPickService` from the supported facet and result controller load paths and from the facet/result probe commands by introducing `SupportedRequestPickSanitizer`, and it no longer uses `QuerySetupBuilder` internally.
- Phase 2 has now removed `QuerySetupBuilder` from composed result projection setup, legacy result projection setup, category-count setup, bogus-pick normalization, and the shared category-info path by introducing `SupportedRequestQuerySetupFactory`, leaving only retained base-class and compatibility uses to clean up later.
- Plugin removal still cannot be done as a single delete step, but the repository now has an explicit DI boundary: the facet plugins expose separate legacy, composer, and shared registration groups.
- `facet.facet_template` is not yet ready for removal solely because it looks legacy. Phase 2 has proven that no non-archived request-path code consumes it, so later work should treat it as importer/schema compatibility cleanup rather than as a live request-path dependency.
- The named top-level `deprecated/*.sql` files do not appear to be active operational dependencies on the current branch and can be treated as archival material unless a later phase uncovers an external workflow that still depends on them.

## Validation Notes

- Cross-checked the runtime inventory against the active deprecation proposal and phase plan.
- Cross-checked SQL asset status against `docs/OPERATIONS.md`, the route-configuration proposal material, and the current importer/runtime schema code.
- Built `sead.query.core` and `sead.query.api` after splitting facet plugin registrations into explicit legacy/composer/shared groups.
- Ran focused Phase 2 tests showing that `SupportedRequestQuerySetupFactory` matches `QuerySetupBuilder` for supported request and result setup, and that `SupportedRequestPickSanitizer` matches `BogusPickService` for `sites:country@10/sites`.
- Ran focused probe, category-info, and DI tests after moving the facet/result probe commands to `SupportedRequestPickSanitizer` and the shared `CategoryInfoService` path to `SupportedRequestQuerySetupFactory`.
- Ran focused category-count, result-handoff, bogus-pick parity, and DI tests after moving `CategoryCountService`, `LegacyResultProjectionHandoffBuilder`, and `BogusPickService` onto `SupportedRequestQuerySetupFactory`.
- Confirmed that the named `deprecated/*.sql` assets have no active references on the current branch beyond the deprecation planning material.